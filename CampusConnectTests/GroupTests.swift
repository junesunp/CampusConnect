//
//  GroupTests.swift
//  CampusConnectTests
//
//  Created by Andy Park on 11/5/21.
//

import XCTest
@testable import CampusConnect
//let db = Firestore.firestore()

class GroupTests: XCTestCase {

    override func setUpWithError() throws {
        // Put setup code here. This method is called before the invocation of each test method in the class.
      super.setUp()
    }

    override func tearDownWithError() throws {
        // Put teardown code here. This method is called after the invocation of each test method in the class.
      super.tearDown()
    }

    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct results.
      
//      let wonho = Student(id: "123", Email:"wonho@gmail.com", First:"Wonho", Last:"Kang", Grad:"2022", Major:"ECE", Phone:"412-512-8888", School:"CMU", Password:"secret", Groups: [])
//
//      let jack = Recruiter(id: "abc", Email:"jack@gmail.com", First:"Jack", Last:"Fuller", Phone:"412-512-9922", Company:"HL", Position:"Analyst", Password:"secret")
//
//      let recDocRef = db.collection("Recruiter").document(jack.id!)
      
//      let barclays = Group(id:"000", Created:Date.now, Updated:Date.now, Name:"Barclays", Description:"Bank", Recruiter: jack, Students: [wonho])
      
//      XCTAssertNotNil(barclays)
//      XCTAssertEqual(barclays.Recruiter.First, "Jack")
//      XCTAssertEqual(barclays.Students[0].First, "Wonho")
    }

}
